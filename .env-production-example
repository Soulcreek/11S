# 11Seconds Quiz Game - Production Configuration
# WICHTIG: Setze hier deine echten Netcup MySQL-Daten ein!

# MySQL Datenbank (DEINE ECHTEN WERTE HIER EINTRAGEN!)
DB_HOST=10.35.233.76
DB_PORT=3306  
DB_USER=quiz_user              ← Dein MySQL Benutzername
DB_PASS=dein_super_sicheres_passwort  ← Dein MySQL Passwort
DB_NAME=quiz_game_db           ← Dein Datenbankname

# Sicherheit (WICHTIG: Ändere diesen Schlüssel!)
JWT_SECRET=mein_super_sicherer_jwt_schluessel_mindestens_32_zeichen_lang_12345

# Server Configuration
PORT=3011
NODE_ENV=production

# Optional: Debug Logging aktivieren
# DEBUG=true
